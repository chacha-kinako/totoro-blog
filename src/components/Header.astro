---
import { SITE_TITLE } from '../consts';
import HeaderLink from './HeaderLink.astro';
---

<header class="site-header">
	<div class="site-header__inner">
		<a href="/" class="site-header__logo">{SITE_TITLE}</a>
		<nav class="site-header__nav">
			<HeaderLink href="/">ホーム</HeaderLink>
			<HeaderLink href="/blog">記事一覧</HeaderLink>
			<HeaderLink href="/about">プロフィール</HeaderLink>
			<a href="/#latest" class="nav-anchor">最新の記事</a>
			<a href="/#popular" class="nav-anchor">人気の記事</a>
			<a href="/rss.xml" class="nav-anchor nav-anchor--rss" target="_blank" rel="noopener">RSS</a>
		</nav>
	</div>
</header>

<style>
	.site-header {
		background: #fff;
		border-bottom: 2px solid var(--green-pale);
		position: sticky;
		top: 0;
		z-index: 100;
		box-shadow: 0 1px 8px rgba(74, 122, 90, 0.07);
	}
	.site-header__inner {
		max-width: 960px;
		margin: auto;
		padding: 0 1.5em;
		display: flex;
		align-items: center;
		justify-content: space-between;
		height: 56px;
		gap: 1em;
	}
	.site-header__logo {
		font-family: 'Kaisei Decol', 'Hiragino Mincho ProN', 'Yu Mincho', serif;
		font-size: 1em;
		font-weight: 700;
		color: var(--green-dark);
		text-decoration: none;
		letter-spacing: 0.05em;
		white-space: nowrap;
		flex-shrink: 0;
	}
	.site-header__logo:hover { color: var(--green); text-decoration: none; }

	.site-header__nav {
		display: flex;
		align-items: center;
		gap: 0.1em;
		flex-wrap: nowrap;
		overflow-x: auto;
	}
	.site-header__nav a {
		font-size: 0.82em;
		font-weight: 600;
		color: var(--text-muted);
		padding: 0.4em 0.7em;
		border-radius: 6px;
		text-decoration: none;
		transition: background 0.15s, color 0.15s;
		white-space: nowrap;
	}
	.site-header__nav a:hover,
	.site-header__nav a.active {
		background: var(--green-pale);
		color: var(--green-dark);
		text-decoration: none;
	}
	.nav-anchor--rss {
		color: var(--acorn) !important;
		border: 1px solid var(--acorn-light);
	}
	.nav-anchor--rss:hover {
		background: var(--acorn-light) !important;
		color: var(--acorn) !important;
	}

	@media (max-width: 680px) {
		.site-header__inner { padding: 0 0.8em; height: 52px; }
		.site-header__logo { font-size: 0.88em; }
		.site-header__nav a { padding: 0.35em 0.5em; font-size: 0.76em; }
	}
</style>
